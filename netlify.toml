[build]
  publish = "."
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"

# 环境变量配置说明
# 在 Netlify 控制台中设置以下环境变量：
# DEEPSEEK_API_KEY = "您的 DeepSeek API 密钥"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# 函数配置
[functions]
  node_bundle = true